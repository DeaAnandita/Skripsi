
<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Rekomendasi</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="p-4"><h1>Daftar Rekomendasi</h1><a href="{{ route('rekomendasi.create') }}" class="btn btn-primary mb-3">+ Tambah</a>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<table class="table table-bordered"><thead><tr><th>ID</th><th>Oleh</th><th>Terkait</th><th>Jenis</th><th>Deskripsi</th><th>Aksi</th></tr></thead><tbody>@foreach($items as $it)<tr><td>{{ $it->id }}</td><td>{{ $it->user->name }}</td><td>@if($it->aset_keluarga_id) Aset Keluarga #{{ $it->aset_keluarga_id }} @elseif($it->aset_lahan_id) Lahan #{{ $it->aset_lahan_id }} @else - @endif</td><td>{{ $it->jenis_rekomendasi }}</td><td>{{ $it->deskripsi }}</td><td><form style="display:inline-block" action="{{ route('rekomendasi.destroy',$it->id) }}" method="POST">@csrf @method('DELETE')<button class="btn btn-danger btn-sm" onclick="return confirm('Hapus?')">Hapus</button></form></td></tr>@endforeach</tbody></table></body></html>
