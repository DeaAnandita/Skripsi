
<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tambah Rekomendasi</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="p-4"><h1>Tambah Rekomendasi</h1><a href="{{ route('rekomendasi.index') }}" class="btn btn-secondary mb-3">Kembali</a>@if($errors->any())<div class="alert alert-danger"><ul>@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif<form action="{{ route('rekomendasi.store') }}" method="POST">@csrf<div class="mb-3"><label>Pemberi (user)</label><select name="user_id" class="form-control">@foreach(App\Models\User::all() as $u)<option value="{{ $u->id }}">{{ $u->name }}</option>@endforeach</select></div><div class="mb-3"><label>Terkait Aset Keluarga (opsional)</label><select name="aset_keluarga_id" class="form-control"><option value="">-- pilih --</option>@foreach(App\Models\AsetKeluarga::all() as $k)<option value="{{ $k->id }}">#{{ $k->id }} - {{ $k->nama_aset }}</option>@endforeach</select></div><div class="mb-3"><label>Terkait Aset Lahan (opsional)</label><select name="aset_lahan_id" class="form-control"><option value="">-- pilih --</option>@foreach(App\Models\AsetLahan::all() as $l)<option value="{{ $l->id }}">#{{ $l->id }} - {{ $l->nama_lahan }}</option>@endforeach</select></div><div class="mb-3"><label>Jenis Rekomendasi</label><input name="jenis_rekomendasi" class="form-control" required></div><div class="mb-3"><label>Deskripsi</label><textarea name="deskripsi" class="form-control" rows="4" required></textarea></div><button class="btn btn-success">Simpan</button></form></body></html>
